# コブツログ 開発指示書

## プロジェクト概要

**サービス名**: コブツログ（KobutsuLog）
**概要**: 古物商向け法令対応の取引台帳管理アプリ
**ターゲット**: せどらー、リサイクルショップ、古物商許可取得者
**コンセプト**: 「法令遵守の最低ラインを3分で記録できる」

---

## 技術スタック

### フロントエンド
- **Flutter** (iOS/Android クロスプラットフォーム)
- Dart 3.x
- 状態管理: Riverpod
- ローカルDB: Hive（オフライン対応）

### バックエンド
- **Cloudflare Workers** (TypeScript)
- **Cloudflare D1** (SQLite)
- **Cloudflare R2** (画像ストレージ)

### 認証
- **Firebase Authentication**
  - メール/パスワード
  - Google Sign-In

### 課金（MVP後）
- **RevenueCat**

---

## DB設計 (Cloudflare D1)

### users テーブル
```sql
CREATE TABLE users (
  id TEXT PRIMARY KEY,
  firebase_uid TEXT UNIQUE NOT NULL,
  email TEXT NOT NULL,
  display_name TEXT,
  business_name TEXT,
  license_number TEXT,
  plan TEXT DEFAULT 'free',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### transactions テーブル
```sql
CREATE TABLE transactions (
  id TEXT PRIMARY KEY,
  user_id TEXT NOT NULL,
  transaction_type TEXT NOT NULL, -- 'buy' or 'sell'
  item_name TEXT NOT NULL,
  item_category TEXT,
  quantity INTEGER DEFAULT 1,
  price INTEGER NOT NULL,
  transaction_date DATETIME NOT NULL,
  -- 相手方情報（法令必須）
  counterparty_name TEXT NOT NULL,
  counterparty_address TEXT NOT NULL,
  counterparty_age INTEGER,
  counterparty_occupation TEXT,
  -- 本人確認
  id_verification_type TEXT NOT NULL,
  id_verification_number TEXT,
  -- その他
  photo_url TEXT,
  notes TEXT,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id)
);
```

### 本人確認方法 (id_verification_type)
- `drivers_license` - 運転免許証
- `health_insurance` - 健康保険証
- `my_number` - マイナンバーカード
- `passport` - パスポート
- `residence_card` - 在留カード
- `other` - その他

---

## API設計 (Cloudflare Workers)

### エンドポイント一覧

| Method | Path | 説明 |
|--------|------|------|
| POST | `/api/auth/register` | ユーザー登録 |
| GET | `/api/users/me` | ユーザー情報取得 |
| PUT | `/api/users/me` | ユーザー情報更新 |
| GET | `/api/transactions` | 取引一覧取得 |
| POST | `/api/transactions` | 取引登録 |
| GET | `/api/transactions/:id` | 取引詳細取得 |
| PUT | `/api/transactions/:id` | 取引更新 |
| DELETE | `/api/transactions/:id` | 取引削除 |
| POST | `/api/upload/image` | 画像アップロード |
| GET | `/api/stats/monthly` | 月次統計 |

### リクエスト/レスポンス例

#### POST /api/transactions
```json
// Request
{
  "transaction_type": "buy",
  "item_name": "iPhone 14 Pro",
  "item_category": "electronics",
  "quantity": 1,
  "price": 80000,
  "transaction_date": "2025-01-15T10:30:00Z",
  "counterparty_name": "山田太郎",
  "counterparty_address": "東京都渋谷区...",
  "counterparty_age": 35,
  "counterparty_occupation": "会社員",
  "id_verification_type": "drivers_license",
  "photo_url": "https://r2.example.com/xxx.jpg",
  "notes": ""
}

// Response
{
  "success": true,
  "data": {
    "id": "txn_xxxxx",
    "created_at": "2025-01-15T10:30:00Z"
  }
}
```

### 認証
- Firebase ID Token を Authorization ヘッダーで送信
- `Authorization: Bearer <firebase_id_token>`

---

## 画面設計

### 1. ホーム画面（一覧）
```
┌─────────────────────────────┐
│ コブツログ          [設定]  │
├─────────────────────────────┤
│ [検索バー]                  │
├─────────────────────────────┤
│ 今月: 32件 / 50件           │
│ ━━━━━━━━━━━━━━━━━━ 64%     │
├─────────────────────────────┤
│ ┌─────────────────────────┐ │
│ │📷 iPhone 14 Pro         │ │
│ │   ¥80,000  買取         │ │
│ │   山田太郎 / 1月15日    │ │
│ └─────────────────────────┘ │
│ ┌─────────────────────────┐ │
│ │📷 ブランドバッグ        │ │
│ │   ¥45,000  買取         │ │
│ │   佐藤花子 / 1月14日    │ │
│ └─────────────────────────┘ │
│         ...                 │
├─────────────────────────────┤
│          [＋ 新規登録]      │
└─────────────────────────────┘
```

### 2. 新規登録画面
```
┌─────────────────────────────┐
│ ← 新規登録                  │
├─────────────────────────────┤
│                             │
│ 取引区分                    │
│ [買取 ▼]                    │
│                             │
│ 品名 *                      │
│ [                        ]  │
│                             │
│ 数量        金額 *          │
│ [1    ]     [         円]   │
│                             │
│ 取引日時 *                  │
│ [2025/01/15 10:30      ]    │
│                             │
│ ─── 相手方情報 ───          │
│                             │
│ 氏名 *                      │
│ [                        ]  │
│                             │
│ 住所 *                      │
│ [                        ]  │
│                             │
│ 年齢        職業            │
│ [    歳]    [            ]  │
│                             │
│ 本人確認方法 *              │
│ [運転免許証 ▼]              │
│                             │
│ 写真                        │
│ [📷 撮影] [🖼 選択]          │
│                             │
│ メモ                        │
│ [                        ]  │
│                             │
│      [ 登録する ]           │
│                             │
└─────────────────────────────┘
```

### 3. 詳細/編集画面
- 登録画面と同様のレイアウト
- 上部に削除ボタン

### 4. 設定画面
```
┌─────────────────────────────┐
│ ← 設定                      │
├─────────────────────────────┤
│                             │
│ アカウント                  │
│ ┌─────────────────────────┐ │
│ │ メールアドレス          │ │
│ │ example@email.com       │ │
│ └─────────────────────────┘ │
│                             │
│ 事業者情報                  │
│ ┌─────────────────────────┐ │
│ │ 屋号                    │ │
│ │ 古物商許可番号          │ │
│ └─────────────────────────┘ │
│                             │
│ プラン                      │
│ ┌─────────────────────────┐ │
│ │ 無料プラン              │ │
│ │ 月50件まで              │ │
│ │ [プレミアムにアップグレード]│
│ └─────────────────────────┘ │
│                             │
│ その他                      │
│ ┌─────────────────────────┐ │
│ │ 利用規約                │ │
│ │ プライバシーポリシー    │ │
│ │ ログアウト              │ │
│ └─────────────────────────┘ │
│                             │
└─────────────────────────────┘
```

---

## ディレクトリ構成

```
kobutsu-log/
├── app/                          # Flutter アプリ
│   ├── lib/
│   │   ├── main.dart
│   │   ├── app.dart
│   │   ├── config/
│   │   │   ├── constants.dart
│   │   │   ├── theme.dart
│   │   │   └── routes.dart
│   │   ├── models/
│   │   │   ├── user.dart
│   │   │   └── transaction.dart
│   │   ├── providers/
│   │   │   ├── auth_provider.dart
│   │   │   ├── user_provider.dart
│   │   │   └── transaction_provider.dart
│   │   ├── services/
│   │   │   ├── api_service.dart
│   │   │   ├── auth_service.dart
│   │   │   └── storage_service.dart
│   │   ├── screens/
│   │   │   ├── splash_screen.dart
│   │   │   ├── login_screen.dart
│   │   │   ├── home_screen.dart
│   │   │   ├── transaction_form_screen.dart
│   │   │   ├── transaction_detail_screen.dart
│   │   │   └── settings_screen.dart
│   │   ├── widgets/
│   │   │   ├── transaction_card.dart
│   │   │   ├── custom_text_field.dart
│   │   │   ├── photo_picker.dart
│   │   │   └── loading_overlay.dart
│   │   └── utils/
│   │       ├── validators.dart
│   │       └── formatters.dart
│   ├── pubspec.yaml
│   └── ...
│
├── api/                          # Cloudflare Workers
│   ├── src/
│   │   ├── index.ts
│   │   ├── routes/
│   │   │   ├── auth.ts
│   │   │   ├── users.ts
│   │   │   ├── transactions.ts
│   │   │   └── upload.ts
│   │   ├── middleware/
│   │   │   ├── auth.ts
│   │   │   └── cors.ts
│   │   ├── services/
│   │   │   ├── firebase.ts
│   │   │   └── r2.ts
│   │   └── utils/
│   │       └── response.ts
│   ├── wrangler.toml
│   ├── package.json
│   └── tsconfig.json
│
└── README.md
```

---

## 実装手順

### Phase 1: 環境構築（Day 1 午前）

1. Flutter プロジェクト作成
```bash
flutter create --org com.kobutsulog kobutsu_log
cd kobutsu_log
```

2. 必要パッケージ追加 (pubspec.yaml)
```yaml
dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.9
  firebase_core: ^2.24.2
  firebase_auth: ^4.16.0
  google_sign_in: ^6.2.1
  dio: ^5.4.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  image_picker: ^1.0.7
  intl: ^0.18.1
  go_router: ^13.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.8
  hive_generator: ^2.0.1
```

3. Cloudflare Workers プロジェクト作成
```bash
mkdir api && cd api
npm init -y
npm install wrangler typescript @cloudflare/workers-types hono firebase-admin
```

4. Firebase プロジェクト設定
- Firebase Console でプロジェクト作成
- Authentication 有効化（Email/Password, Google）
- FlutterFire CLI で設定ファイル生成

### Phase 2: バックエンド実装（Day 1 午後）

1. D1 データベース作成・マイグレーション
2. Workers API エンドポイント実装
3. Firebase Auth 連携
4. R2 画像アップロード実装

### Phase 3: フロントエンド実装（Day 2）

1. 認証フロー（ログイン/登録）
2. ホーム画面（一覧表示）
3. 新規登録画面
4. 詳細/編集画面
5. 設定画面

### Phase 4: 統合・テスト（Day 2 夜）

1. API連携確認
2. 画像アップロード確認
3. オフライン動作確認
4. エラーハンドリング

---

## MVP制限事項

| 項目 | 無料プラン制限 |
|------|---------------|
| 月間登録件数 | 50件 |
| 写真 | 1件につき1枚 |
| データ保存期間 | 3年 |
| エクスポート | 不可 |

制限チェックはフロントエンド + バックエンド両方で実装

---

## 注意事項

1. **法令必須項目**は必ず入力必須にする
   - 品名、金額、取引日時
   - 相手方：氏名、住所
   - 本人確認方法

2. **個人情報の取り扱い**
   - 相手方情報は暗号化して保存
   - HTTPS通信必須

3. **オフライン対応**
   - Hiveにローカル保存
   - オンライン復帰時に同期

4. **画像最適化**
   - アップロード前に圧縮（max 1MB）
   - R2に保存、URLをDBに記録

---

## デプロイ

### Cloudflare Workers
```bash
cd api
npx wrangler deploy
```

### Flutter App
```bash
# Android
flutter build appbundle --release

# iOS
flutter build ios --release
```

---

## 成功基準

- [ ] ユーザー登録/ログインできる
- [ ] 取引を登録できる
- [ ] 一覧表示・検索できる
- [ ] 写真を添付できる
- [ ] 編集・削除できる
- [ ] 50件制限が機能する
- [ ] オフラインで閲覧できる
